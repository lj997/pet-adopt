server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pet_adopt?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: test
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

file:
  upload:
    path: uploads/

jwt:
  secret: PetAdoptSecretKeyForJWTToken2024VeryLongSecretKey
  expiration: 86400000
